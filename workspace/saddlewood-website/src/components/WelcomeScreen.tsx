"use client";

import React from 'react';

/**
 * Welcome screen component - now disabled for all visitors
 * 
 * This component has been disabled as requested, and will never display
 * the welcome animation for any visitor, first-time or returning.
 */
export default function WelcomeScreen() {
  // Always return null to disable the welcome screen for all visitors
  return null;
}